import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    button:{
        padding: 10,
        color: "#000000",
        backgroundColor: "#ffda00",
        borderRadius:7.5,
    }
})

export {styles}
